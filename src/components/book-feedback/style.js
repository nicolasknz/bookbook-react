import styled from 'styled-components'
import { StyledButton } from "../styled/styled-button";

export const ModalButton = styled(StyledButton)`
  width: 80px;
`;

export const ModalFormButton = styled(StyledButton)`
  width: 140px;
`;
