import { StyledAd, StyledAdTitle, StyledAdText, StyledAdFooter } from './styled-advertising';
import StyledCard from './styled-card';
import Container from './styled-container';
import { StyledEdit, ModalTitle } from './styled-edit-user';
import StyledHeadProfile from './styled-profile';
import StyledTimeline from './styled-timeline';

export {
  Container,
  StyledCard,
  StyledTimeline,
  StyledHeadProfile,
  StyledEdit,
  ModalTitle,
  StyledAd,
  StyledAdTitle,
  StyledAdText,
  StyledAdFooter,
};
